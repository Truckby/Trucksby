
newseller@gmail.com
12345678

Truckby
New features: 


Changes:
- In contact us: link mailto: on mail. --



Done:
- remove:  the slideshow automatic feature, on all listings so buyers can only see next pictures when they click the arrow
- Find out how to make all listing squares same size. When the text is longer the pictures get smaller. 
- Home dropdown search find out how to make all names fit the square (maybe something like mobile version)
- Make all listing names CAPITAL LETTERS. Dont let ppl input anything in lowercase so all our listing names are exactly the same and the page looks uniform. 
- Change name of contact us button on listings to “Contact Seller”
- Remove the “description” on description field. 
- Add commas on mileage on listing views --
- Change name of listing to listings on footer.
- Home on footer if clicked and you are at the bottom of home make it go TOP HOME (always top home) 
- Truckby logo in footer to take you TOP HOME always.
- add company name with a suitcase icon (🧳) (contact information) 
-  change icon of address to the same pin we have in location📍 (contact information)
- Drag and Drop images to arrange order and profile pic of the listing.
- if $0 on price show Contact For Pricing (IN RED) and let the seller know that if $0 it will show contact for pricing. 
- Change email subject: (From: New message for Customer Support To: New Lead from Truckby.com) 
- change email title: ( New message for Customer Support To: New Lead from Truckby.com)
- subscription to newsletter is giving error.
- Add a note: please contact sales for more than 50 listings*  (on Plans)
- inventory view, to share like truckpaper https://www.truckpaper.com/listings/for-sale/supermixers-orlando-florida/trucks-and-trailers?LocationID=350013493804



https://www.npmjs.com/package/@hello-pangea/dnd


    reorderProjects: (state, action) => {
      const { sourceIndex, destinationIndex } = action.payload;
      const [movedProject] = state.projects.splice(sourceIndex, 1);
      state.projects.splice(destinationIndex, 0, movedProject);
      console.log('Projects reordered:', current(state.projects));
      saveProjectsToLocalStorage(state.projects);
    }

                <DragDropContext onDragEnd={handleOnDragEnd}>
                  <Droppable droppableId="droppable-list">
                    {(provided) => (
                      <div {...provided.droppableProps} ref={provided.innerRef} className="space-y-3">
                        {projects.map((project, index) => (
                          <Draggable key={project.id} draggableId={project.id} index={index}>
                            {(provided) => (
                              <div
                                ref={provided.innerRef}
                                {...provided.draggableProps}
                                {...provided.dragHandleProps}
                                style={{
                                  ...provided.draggableProps.style,
                                }}
                              >
                                <ProjectCard
                                  key={project.id}
                                  project={project}
                                  isActive={activeProject?.id === project.id}
                                  onClick={() => handleProjectSelect(project)}
                                  currentTime={currentTime}
                                  currentMode={currentMode}
                                  isBreak={isBreak}
                                />
                              </div>
                            )}
                          </Draggable>
                        ))}
                        {provided.placeholder}
                      </div>
                    )}
                  </Droppable>
                </DragDropContext>